# Proto Contract Notes

SROS V2 remains local-only and CLI-first.
Transport protocol contract generation is intentionally deferred.
This directory exists to reserve protobuf contract ownership for workflows that require it.
