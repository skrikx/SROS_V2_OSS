# Local Ask Mode

Local ask-mode resolves through CLI-visible approval artifacts.

- An ask decision writes a JSON approval artifact under the local runtime artifacts tree.
- Resume requires explicit operator action and a positive approval artifact.
- No hosted approval surfaces or enterprise review centers are introduced in W06.
