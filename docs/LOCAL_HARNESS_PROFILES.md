# Local Harness Profiles

Built-in profiles:

- `read_only`
- `patch_only`
- `shell_gated`
- `container_sandbox`
- `network_restricted`
- `elevated_break_glass`

The harness resolves the first declared compatible profile and blocks activation when none are compatible.
