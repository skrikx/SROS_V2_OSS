# Replay and Receipts

- `sros replay` copies or replays evidence artifacts into `artifacts/replays`
- `sros receipts export` exports receipt bundles
- `sros release pack` creates a local release baseline artifact
