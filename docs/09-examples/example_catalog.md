# Example Catalog

Primary categories:

- runs
- tools
- traces
- receipts
- releases
- regression

Use:

- `sros examples catalog`
- `sros examples run`

The catalog is stored at `examples/catalog.json`.
