# GOV Plane

GOV owns Allow, Ask, Deny decisioning for privileged runtime actions.

- Every privileged capability is classified to a trust boundary before evaluation.
- Sandbox profiles are explicit and capabilities without declared compatibility are denied.
- Shell and patch default safe. They require explicit policy meaning.
- Break-glass is narrow, explicit, and auditable.
