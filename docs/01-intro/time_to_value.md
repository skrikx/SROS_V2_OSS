# Time To Value

The first hour should produce at least one of these:

- a verified front-door readiness report
- a trace inspection from a real example artifact
- a receipt bundle export
- a release-pack artifact
- a generated showcase pack under `artifacts/showcase`

The fastest path is `scripts/first_run_smoke.sh`, backed by the same CLI surfaces used everywhere else.
