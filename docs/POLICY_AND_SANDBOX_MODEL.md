# Policy And Sandbox Model

Policy bundles provide governance meaning for runtime capabilities.

- Capability rules map named capabilities to Allow, Ask, or Deny plus trust-boundary and sandbox expectations.
- Sandbox profiles declare whether shell, patch, or external network actions are compatible.
- W06 governs capability semantics only. Connector execution, MCP transport, and tool invocation remain deferred to W09.
