# Trace Event Model

- Trace events bind run id, trace id, span id, event type, payload, artifact refs, and optional receipt refs.
- State transitions, policy decisions, memory mutations, mirror witnesses, receipt links, and closure proofs are explicit event classes.
