# Local Bootstrap

- Copy `.env.example` or export the same variables.
- Start local PostgreSQL with `compose.yaml` if desired.
- Run `scripts/bootstrap_local.sh`.
- Run `scripts/migrate_local.sh`.
- Run `scripts/seed_local.sh`.
