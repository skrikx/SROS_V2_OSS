# Showcase Pack

The showcase pack exists to make sharing easy without flattening the machine.

Generate it with:

- `scripts/build_showcase_pack.sh`

The output includes:

- example catalog summary
- first-run snapshot
- share pack manifest
