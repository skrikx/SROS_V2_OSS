# Local Smoke

Smoke validates the real local machine surfaces:

- bootstrap and scaffold
- manifest validation and registry search
- connector envelope inspection
- MCP ingress normalization
- verify, replay, and release pack commands
